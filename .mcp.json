{
  "mcpServers": {
    "commons": {
      "command": "commons-mcp",
      "args": [
        "--audience",
        "store"
      ]
    }
  },
  "$comment": "The shared layer over the other repos, scoped to this one: audience `store`. The tool set and the corpus projection are fixed by that audience and cannot be widened from a call \u2014 see portfolio-commons/src/portfolio_commons/audiences.py. Read-only by design: writes go through the owning repo's CLI or a branch and a PR."
}
